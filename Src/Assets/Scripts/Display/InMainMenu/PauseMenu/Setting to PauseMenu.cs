using UnityEngine;

public class SettingtoPauseMenu : MonoBehaviour
{
    public GameObject Setting_Button;

    // Start is called before the first frame update
    public void Setting()
    {
        Setting_Button.SetActive(true);
    }
}
